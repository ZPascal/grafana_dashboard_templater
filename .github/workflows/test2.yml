name: PR checks Test 2

on:
  pull_request:
    branches: [ main ]

jobs:

  dummy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ '3.x' ]

    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      
      - name: Generate file
        run: rm docs/test1.txt && echo test1 > docs/test1.txt

      - name: "Check if file existence"
        id: check_files
        uses: andstor/file-existence-action@v1
        with:
          files: "docs/test1.txt"
      
      - name: Commit files
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add docs --force
          git commit -m "Test the action"

      - name: Push changes
        uses: ad-m/github-push-action@4a47af8c1f0050f78e7d70ca0580f360c2dace98
        with:
          force_with_lease: true
